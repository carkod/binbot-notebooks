# jupytext.toml
default_jupytext_formats = "ipynb,py:percent"
# Or restrict to specific notebook dirs:
# formats = "binbot-notebooks//*.ipynb,binbot-notebooks//*.py:percent"

# Cell metadata filtering to avoid noise:
notebook_metadata_filter = "kernelspec,language_info"
cell_metadata_filter = "-all"
